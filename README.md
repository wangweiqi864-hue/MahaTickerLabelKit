# MahaTickerLabelKit

MahaTickerLabelKit is the app's private ticker-style numeric label component.

It preserves the displayed counting behavior while exposing app-specific APIs:

- `MAHA_TickerValueLabel`
- `mahaTickerPattern`
- `mahaTickerComposer`
- `mahaShiftValue(_:cycleSpan:)`
